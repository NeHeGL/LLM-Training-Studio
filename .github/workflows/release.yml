name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g. v1.2.3)'
        required: true

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Create and push tag
        run: |
          git tag ${{ inputs.version }}
          git push origin ${{ inputs.version }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.3.2
        with:
          tag_name: ${{ inputs.version }}
          name: "LLM Training Studio ${{ inputs.version }}"
          generate_release_notes: true
          body: |
            ## What's New in ${{ inputs.version }}

            See the [Changelog in README.md](https://github.com/NeHeGL/LLM-Training-Studio/blob/${{ inputs.version }}/README.md#changelog) for full release notes.

            ---

            ### Quick Start
            1. Download **Source code (zip)** below
            2. Extract anywhere on your machine
            3. Run `install.bat` once to set up dependencies
            4. Launch with `start_web.bat` (browser) or `start_app.bat` (desktop window)
